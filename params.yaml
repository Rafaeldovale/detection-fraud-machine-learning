# Configuration parameters for the machine learning pipeline
data:
  raw_path: "data/raw/creditcard.csv"

train:
  test_size: 0.2
  random_state: 42
  logistic_regression:
    max_iter: 1000
  random_forest:
    n_estimators: 100
